spring:
     #  zipkin:
     #      base-url: http://localhost:9411
     #  sleuth:
     #    sampler:
     #    #采样率值介于 0 到 1 之间，1 则表示全部采集
     #    probability: 1
     datasource:
          type: com.alibaba.druid.pool.DruidDataSource            # 当前数据源操作类型
          driver-class-name: org.gjt.mm.mysql.Driver               # mysql驱动包
          url: jdbc:mysql://118.89.108.13:3306/springcloud?useUnicode=true&characterEncoding=utf-8&useSSL=false
          username: root
          password: 123456
mybatis:
     mapperLocations: classpath:mapper/*.xml
     type-aliases-package: com.peanut.cloud.entities    # 所有Entity别名类所在包


config:
     info: config-dev.yml333
